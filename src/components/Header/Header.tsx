import "./Header.sass";

const Header: React.FC = () => {
    return (
        <div className="header__wrapper">
            <h1 className="header__title"> Inforce shop</h1>
        </div>
    );
};

export default Header;
